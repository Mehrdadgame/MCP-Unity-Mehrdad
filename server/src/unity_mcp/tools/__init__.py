"""Tool registrations.

Phase 1 ships only the smoke tools, which live in ``unity_mcp.server``.
The ``_v1`` and ``_v2`` subpackages get populated in later phases as each
handler category comes online on the Unity side.
"""
